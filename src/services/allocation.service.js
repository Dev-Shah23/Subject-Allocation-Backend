// Allocation service placeholder
// TODO: implement allocation logic

module.exports = {
  allocate: async (facultyList, preferences) => {
    // return allocation result
    return { allocations: [] };
  }
};
